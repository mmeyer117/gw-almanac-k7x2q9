{
  "_comment": "Public persona names (male, initials match the real roster by request). The real-name mapping lives in config/private.local.json (gitignored) or the PRIVATE_CONFIG_JSON secret — never in the repo.",
  "personas": ["Porter", "Archie", "Jonas", "Eli", "Dexter", "Max"]
}
